{
    "version": 2,
    "builds": [
        {
            "src": "/flask_app/",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/flask_app/"
        }
    ]
}
